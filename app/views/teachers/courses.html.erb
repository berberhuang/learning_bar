<div class="row">
	<div class="columns">
	<h1>學習履歷</h1>
		<div class="course_list">
		<% @courses.each do |c|%>
		<div class="columns">
			<h4><%= link_to c.name, '/courses/'+c.id.to_s %></h4>
			<div class="large-3 columns"><%=c.course_date%></div>
			<div class="large-3 columns"><%=c.location%></div>
			<div class="large-3 cloumns"><%=link_to :參與名單, attendee_info_course_path(c)%></div>
		</div>
		<% end %>
		</div>
	</div>
</div>
