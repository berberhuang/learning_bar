<div class="row">
	<div class="columns">
	<h1>學習履歷</h1>
	<!--table>
		<tr>
			<th>課程名稱</th>
			<th>課程日期</th>
			<th>課程地點</th>
			<th>報名狀況</th>
		</tr-->
		<% @course.each do |c|%>
		<!--tr>
			<th><h4><%= link_to c.name, '/course/'+c.id.to_s %></h4></th>
			<th><%=c.course_date%></th>
			<th><%=c.location%></th>
			<th><%=link_to '退報名', '/course/cancel_attendence/'+c.id.to_s%></th>
		</tr-->
		<div class="columns">
			<h4><%= link_to c.name, '/course/'+c.id.to_s %></h4>
			<div class="large-4 columns"><%=c.course_date%></div>
			<div class="large-4 columns"><%=c.location%></div>
			<div class="large-4 columns"><%=link_to '退報名', '/course/cancel_attendence/'+c.id.to_s%></div>
		</div>
		<div class="columns">
			<h4><%= link_to c.name, '/course/'+c.id.to_s %></h4>
			<div class="large-4 columns"><%=c.course_date%></div>
			<div class="large-4 columns"><%=c.location%></div>
			<div class="large-4 columns"><%=link_to '退報名', '/course/cancel_attendence/'+c.id.to_s%></div>
		</div>
		</table>

		<% end %>
		
	</div>
</div>
