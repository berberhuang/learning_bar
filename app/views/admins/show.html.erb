<h1><%=@admin.username%></h1>
<% @courses.each do |c|%>
<div class="columns">
	<h4><%= link_to c.name, '/course/'+c.id.to_s %></h4>			
	<div class="large-4 columns"><%=c.course_date%></div>
	<div class="large-4 columns"><%=c.location%></div>
	<div class="large-4 columns"><%=link_to '編輯課程', '/course/edit/'+c.id.to_s%></div>
</div>
<%end%>