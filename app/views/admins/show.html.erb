<h1><%=@admin.username%></h1>
<div><%=link_to '公司列表',:companies %></div>

<% @courses.each do |c|%>
<div class="columns">
	<h4><%= link_to c.name, '/courses/'+c.id.to_s, :class=>'course_name' %></h4>			
	<div class="large-4 columns"><%=c.course_date%></div>
	<div class="large-4 columns"><%=c.location%></div>
	<div class="large-4 columns"><%=link_to '編輯課程', edit_course_path(c)%></div>
	<div class="large-4 columns"><%=link_to '檢視參加名單', attendee_info_course_path(c)%></div>
</div>
<%end%>
<div><%=link_to '增加課程',:new_course %></div>
